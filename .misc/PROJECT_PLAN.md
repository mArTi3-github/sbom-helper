# PURL Source Repository Resolver — План развития

## 1. Цель проекта

Веб-приложение и API-сервис для сопоставления Package URL (PURL) с соответствующим репозиторием исходного кода.

Система должна:
- принимать один PURL и возвращать repository URL;
- поддерживать максимально возможное количество экосистем;
- предоставлять удобный REST API;
- предоставлять web UI;
- поддерживать дальнейшее расширение архитектуры и подключение новых резолверов в будущем.

---

## 2. Этапы разработки

### Выполнено

- FastAPI с endpoint `POST /api/v1/resolve`
- GET /health
- GET / (HTML-страница с формой + карточка результата)
- Интеграция purl2repo
- Интеграционные тесты (pytest, hermetic — без сети)
- Docker multi-stage Dockerfile (dev + prod)
- Docker Compose (app + PostgreSQL)
- PostgreSQL для хранения результатов (lookup до purl2repo)
- Service Layer (оркестрация: cache → resolver → store)
- Storage Layer (interface + PostgresCache + InMemoryCache)
- Graceful degradation при отказе БД
- Multi-resolver архитектура (список резолверов в `app.state.resolvers`)
- Application-level PURL validation (purl_utils, packageurl-python)
- Normalized cache keys (`scheme:type/namespace/name`)
- FakeResolver для hermetic тестов
- ADRs: purl2repo, PostgreSQL, purl validation
- **Пакетная обработка PURL** — `resolve_batch()` в service layer (параллельный резолвинг с semaphore)
- **Обогащение SBOM** — приём CycloneDX JSON, резолвинг компонентов, вставка VCS-ссылок
- **Кэширование результатов обогащения** — PURL из обогащённых SBOM автоматически сохраняются в БД (включая компоненты с уже заполненными externalReferences)
- **Database Admin** — веб-интерфейс `/db-admin` для просмотра, редактирования, фильтрации, импорта/экспорта (CSV с `;`-разделителем) и массового удаления записей `resolved_purls`
- **Удаление лишних файлов из проекта** — проверка необходимости и достаточности набора файлов в проекте и удаление устаревших/избыточных файлов спецификаций/конфигураций/кода
- **Добавление асинхронной обработки запросов пользователей** - нужно, чтобы с проектом могли одновременно работать несколько пользователей. Для этого требуется, чтобы задачи выполнялись в асинхронном режиме, и выполнение одной задачи не блокировало обработку других параллельных запросов
- **Изменить логику проверки доступности github.com** — проверять доступность не при каждом запросе проверки корректности ссылки из локальной БД, а только перед началом обработки первого запроса
- **Добавление возможности генерации машиночитаемого  описания списка docker-образов по требованиям ФСТЭК** - это возможно сделать на основе SBOM, но останется ручная часть, связанная с описанием назначения образов
- **Миграция фронтенда на Vue.js** — перевод с Vanilla JS на Vue.js для обеспечения более красивой архитектуры и более удобного взаимодействия с проектом
- **Исправить недостатки раздела DB Admin** - из-за перевода фронтенда на Vue.js появилось несколько мелких недостатков, которые требуется исследовать и исправить:
  - кнопка "Apply" не работает, а также ее функционал теперь дублируется с кнопкой "Refresh", оставить только Apply?
  - все столбцы таблицы не помещаются по ширине, сделать таблицу на всю ширину страницы
- **Исправить кнопку Export CSV** - сейчас нажатие на кнопку приводит к ошибке "HTTP 422"
- **Перенаправления в ссылках** - Разобраться, как происходит обработка ссылок, которые возвращают HTTP-код 301 (Moved Permanently). Нужно, чтобы в итоге возвращалась и сохранялась везде ссылка, на которую перенаправляет редирект
- **Исправить зону для перетаскивания файлов в разделе Import CSV** - сейчас там указано, что нужен формат "CycloneDX JSON, до 200 МБ", а нужен формат CSV. Кроме того, в CSV Format Reference указан некорректный пример формата данных
- **Убрать кнопку Save из настроек** - нужно, чтобы настройки применялись сразу, без необходимости нажимать на кнопку Save отдельно
- **Добавить тесты для фронтенда** - т.к. фронтенд переведен на Vue.js, нужно добавить тесты для соответствующего кода
- **Поддержка различных типов репозиториев** - добавить поддержку репозиториев, которые поддерживает sbom-checker (Subversion, Mercurial, Hg, fossil) в дополнение к git-репозиториям

### Очередные задачи

- **LLM-резолвер** — интеграция LLM как дополнительного резолвера
- **Ручное добавление записей в БД** — добавление записей в БД вручную через веб-интерфейс вкладки db-admin
- **Проверить корректность данных в БД** - выяснить, какие данные хранятся в столбцах "Resolver" (проверить, чтобы было корректное мнжество ожидаемых значений), "Repository URL" (проверить, все ли ссылки начинаются с "HTTP"), "Kind" (сейчас для всех строк должно быть указано "VCS"), "Version Ref" (планируется к удалению в будущем), Evidence. Учесть, что столбцы "Type", "Confidence", "Version Ref", "Evidence" планируются к удалению в будущем
- **Удаление лишних полей из БД** — анализ и удаление тех полей из локальной БД, которые не используются при практической работе с проектом
- **Импорт ссылок из SBOM** — добавление к разделу "db-admin" функции "Импорт из SBOM" для импорта сопоставлений PURL -> repository_url в локальную БД (по аналогии с функцией "импорт из CSV")
- **Улучшить логирование** — требуется, чтобы отправка отладочной информации в процессе работы компонентов продукта была единообразной, понятной и достаточной для проведения отладки
- **Добавление прямых ссылок на скачивание исходных текстов, когда нет репозитория** - для случаев, когда нет ссылки на репозиторий, но есть ссылка на скачивание архива с исходными текстами (такая ситуация наблюдается, например, для пакетов типа conda в libraries.io) - добавлять прямую ссылку на скачивание. При этом столбец "repository_kind" переформатировать. У этого столбца должно быть 2 возможных значения - "vcs" (для git-репозиториев) или "source_distribution" (для прямых ссылок на скачивание архивов с исходными текстами)
- **Динамический UI при обогащении SBOM** — присылать отчет о статистике обработки ссылок не в конце обработки, а заполнять его в процессе обработки и динамически обновлять инофрмацию в UI
- **Обертки для инструментов sbom-checker** — добавить обертку в веб-интерфейсе для консольных инструментов для работы со SBOM от ИСП РАН (https://gitlab.community.ispras.ru/sdl-tools/sbom-checker) 
- **Добавление раздела для обработки списка PURL в UI** — кажется, в API была предусмотрена "ручка" /resolve/batch для этого, нужно перепроверить. В веб-интерфейсе этот функционал нуждно добавить в раздел "PURL Resolver" (отдельный раздел не требуется). В поле ввода должа появиться возможность вставлять несколько PURL (по одному PURL в строке, и если пользователь вставялет несоклько строк, то в ответ возвращается информация по каждому запрошенному PURL - в виде таблицы)
- **Отмена обработки SBOM при закрытии страницы** - сейчас, если пользователь запустил задачу обработки SBOM, то обработька продолжается даже если пользователь закрыл вкладку и больше не сможет получить результаты. Нужно, чтобы задача по обработке SBOM отменялась, если пользователь закрывает вкладку в браузере
- **Кэш проверенных ссылок** - сейчас одни и те же ссылки могут проверяться множество раз, если при обработке SBOM установлена галочка "Проверять существующие VCS-ссылки в SBOM" (при условии, когда в SBOM присутствует нгесколько одинаковых ссылок). Нужно, чтобы после первой проверки ссылки информация о том, что ссылка актуальна, записывалась в какой-то кэш, и далее если возникает необходитмость проверить ссылку, то сначала проверяется кэш, а реальная проверка (HTTP HEAD / git ls-remote) осуществляется только для тех ссылок, которые не обнуружатся в кэше
- **Добавить E2E-тесты**
- **Оформить красивый дизайн для фронтенда** - использовать специальный скилл (https://www.skills.sh/anthropics/skills/frontend-design) для оформления дизайна. Также добавить подготовленный значок для проекта. Замеченные недостатки:
  - Перевести все страницы на английский (сейчас часть на русском, часть - на английском)
  - в разделе "SBOM-Updater" кнопку "Скачать обогащенный SBOM" разместить над таблицей
  - Раздел настроек - разместить справа
- **Резолверы для alpine и debian** - добавить какой-то механизм поиска для пакетов с type "apk" и "deb". Вероятно, для apk подойдет единая ссылка https://github.com/alpinelinux/aports/ (либо читать содержимое файла "main/<package_name>/APKBUILD" и искать в нем строку "source=<ссылка на исходные тексты>"). Однако, alpine иногда добавляет собственные патчи к исходникам из апстрима, а также ссылка в source является прямой ссылкой на скачивание, и не всегда является git-репозиторием. Поэтому, неаверное лучше использолвать просто ссылку на aports. А для пакектов с type=deb нужно использовать поиск по gitlab-репозиторию https://salsa.debian.org/
- **Проверка ссылок при резолве** - перед тем, как возвращать ссылку от резолвера, нужно проверять, является ли эта ссылка VCS-репозиторием (сейчас выдаются некорректные результаты, например, для ссылки pkg:pypi/docutils) 
- **Изменить логику проверки доступности интернета** - вместо проверки доступности с помощью обращения к github.com (которое сейчас происходит слишком часто - кажется, перед каждым запросом валидации URL) нужно проверять доступность github.com, когда есть подозрения в проблемах с интернетом (например, для проверяемой ссылки вернулся статус о таймауте подключения или других сетевых проблемах)
- **Упростить логику валидации ссылок** - сейчас соответствующий код кажется запутанным и не очень логичным. Возможно, стоит провести рефакторинг с целью упрощения алгоритма использования функции validate_url_with_retry
- **Добавить функционал по проверке корректности заполнения ГОСТ-полей** - уже готов образец в виде консольного приложения. Также можно добавить доп. функции по заполнению ГОСТ-полей, если поля не заполнены
- **Хранить списолк альтернытивных ссылок для PURL** - для случаев, когда, например, в SBOM обнаружен URL, отличающийся от хранящегося в локальной БД - сохранять его в лоакльной БД в этот список альтернативных URL. Если основная ссылка в локальной БД перестает работать - пробовать заменять на первую ссылку из списка альтернативных ссылок. Предварительно - добавить в локальную БД дополнительный столбец "alternative_urls_lits", в котором должны содержаться дополнительные корректные ссылки, соответствующие указанному PURL (для случаев, если основная ссылка перестанет быть корректной). При этом информация в прочих столбцах ("Resolver", "Resolved at" и прочие) должна по-прежнему относиться к основной ссылке из столбца "Repository URL" и не должна перезаписываться при добавлении альтернативных ссылок

### Отложенные задачи

- **Добавление nginx как reverse-proxy для HTTPS** - если продукт будет усложняться, то логику взаимодействия по HTTPS лучше вынести в отдельный контейнер (reverse-proxy)
- **Попробовать Mint** - разобраться на примере текущего преокта, как работает минификация docker-образов с помощью minT
- **Добавить поддержку выбора языка** - русский/английский, управление в разделе Settings (i18n?)
- **Единое место хранения настроек** - настройки, регулируемые переменными окружения (PURL2REPO_* и т.д.) - перенести в JSON-файл и в раздел настроек приложения. Также можно обсудить возможность сохранения этих настроект в ОЗУ не через переменные кеша, а через перемнные окружения

## 3. Долгосрочные планы по добавлению LLM-резолвера

### Описание

Подключение LLM (Large Language Model) в качестве резолвера для поиска репозитория по PURL. LLM может выполнять поиск в интернете и анализировать результаты, что особенно полезно для редких экосистем или пакетов, которые purl2repo не находит.

### Ключевые вопросы

- Какая LLM используется (GPT, Claude, локальная)?
- Как LLM получает доступ к интернет-поиску (search tool, browsing)?
- Какой формат промпта для LLM?
- Как обрабатывать неструктурированный ответ LLM (парсинг repository_url из текста)?
- Какой timeout для LLM-запроса?
- Нужен ли кэш для результатов LLM (отдельно от основного storage)?
- Должен ли LLM-резолвер использоваться как fallback после purl2repo или параллельно?
- Как оценивать confidence для результатов LLM?

### Зависимости

- Пакет для работы с LLM API (openai, anthropic и т.п.)
- Инструмент для веб-поиска (или встроенный browsing в LLM)

---